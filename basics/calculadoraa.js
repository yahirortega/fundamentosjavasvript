{
    const restar =() =>{
        const values = getValues();
        const resultado = values[0] - values[1];
        result.value=resultado;
    }
const dividir =() =>{
    const values= getValues();
    const resultado = values[0] / values[1];
    result.value=resultado;

}   
const multiplicar =() =>{
    const values= getValues();
    const resultado = values[0] * values[1];
    result.value=resultado;
} 

const limpiar = () =>{
    num1.value=0;
    num2.value=0;
    result.value=0;

}
}